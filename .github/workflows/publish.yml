name: Publish

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - run: bun install

      - name: Test (core)
        run: bun test
        working-directory: packages/core

      - name: Test (render)
        run: bun test
        working-directory: packages/render

      - name: Build (verify compile)
        run: bun run build

      - name: Bundle for npm
        run: bun run build:bundle

      - name: Set version from release tag
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          bun run --filter '*' exec -- sh -c 'tmp=$(mktemp) && jq --arg v "'"$VERSION"'" ".version = \$v" package.json > "$tmp" && mv "$tmp" package.json' || true
          tmp=$(mktemp) && jq --arg v "$VERSION" '.version = $v' package.json > "$tmp" && mv "$tmp" package.json

      - name: Publish to npm
        run: npm publish --access public
